#include "main.h"
/**
 * _abs - calculates the absolute alue of integers
 *
 * Return: the absolute value of int
 */
int _abs(int q)
{
	if (q <  0)
	{
	return (q * (-1));
	}
	else if (q == 0);
	{
	return(0);
	}
	else
	{
	return (q);
	}
}
